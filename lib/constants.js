const EV_STORAGE_CONNECTED = 'EV_STORAGE_CONNECTED'
const CRAWL_MODE = 'CRAWL_MODE'

module.exports =  {
    EV_STORAGE_CONNECTED,
    CRAWL_MODE,
    URI:{
        STATUS_UNCRAWELD:0,
        STATUS_CRAWELD:100,
    }
}
